function arrToStrToArr(arr) {
    const toStr = String(arrayOfCars);
    const backToArr = toStr.split(',');
    console.log(backToArr)
  }
  
  const arrayOfCars = ['car1', 'car2', 'car3', 'car4'];
  arrToStrToArr(arrayOfCars)
  